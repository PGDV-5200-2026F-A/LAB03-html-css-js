# LAB 03

## The one with more `css` and `JavaScript`

### TODO
- Fork and clone the repository
- Open repository in VSCode and enable the Live Server extension
- Add `css` styles to `01-exercise/news.html`
  - Since the elements are created dynamically with `JavaScript`, one way to see the resulting `html` structure is by using the browser's element inspector tool
  - All `html` elements are there and in a sensible, initial structure that can be styled as-is
  - Feel free to add extra elements if it helps adjust the design
